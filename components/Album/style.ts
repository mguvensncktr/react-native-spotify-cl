import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: 125,
        margin: 10

    },
    image: {
        width: '100%',
        height: 125,
        borderRadius: 10

    },
    text: {
        marginTop: 10,
        color: 'grey',
    }

})

export default styles;
